package org.poo.commands;

public interface Command {
    /**
     * Executes the command
     * The implementation should define the specific actions of the command
     */
    void execute();
}
